import React from 'react'

const Heading4 = ({text,className}) => {
  return (
    <h4 className={`text-[25px] text-dark font-bold font-inter ${className}`}>{text}</h4>
  )
}

export default Heading4
